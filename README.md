# Regex-to-NFA-Converter
This project implements a complete Regular Expression to NFA (Non-Deterministic Finite Automaton) converter using Thompson’s Construction Algorithm. The entire implementation is written from scratch without using any external libraries, following the project requirements.
